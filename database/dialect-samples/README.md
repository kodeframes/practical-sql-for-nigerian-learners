# Dialect Samples

This directory is reserved for concise, justified differences among PostgreSQL, SQLite, MySQL 8.4 LTS, and SQL Server 2025.

Do not create four parallel versions of every chapter query. Add a dialect sample only when the difference materially affects correctness or portability.
