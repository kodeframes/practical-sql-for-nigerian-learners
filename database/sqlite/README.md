# SQLite Notes

SQLite is a secondary comparison engine for selected portability lessons. It is **not** the canonical executable platform for the book.

The PostgreSQL schema uses features and semantics that should not be mechanically translated and treated as equivalent. Any SQLite sample in this repository must be labelled explicitly as a dialect sample and tested independently.
