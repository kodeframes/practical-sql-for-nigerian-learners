# Chapter SQL

These scripts are the repository's **canonical runnable chapter examples**. They are intentionally narrower than every SQL-looking fragment printed in the book. Syntax fragments, deliberate mistakes, pseudo-code, and destructive demonstrations are not promoted to canonical runnable status.

Run all scripts only after the MarketLedger database has been reset and seeded. Scripts that demonstrate data modification use transactions or temporary objects so the canonical dataset remains unchanged.
