SELECT branch_name FROM branches;
SELECT branch_code, branch_name, city, state FROM branches ORDER BY branch_id;
SELECT product_id, product_name, unit_price FROM products ORDER BY product_id;
SELECT 2 + 3 AS result;
SELECT 'MarketLedger' AS project_name;
