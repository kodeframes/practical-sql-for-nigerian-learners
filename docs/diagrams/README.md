# Diagrams

- `marketledger-er.mmd` is the canonical Mermaid ER source for the MarketLedger schema.

The source is intentionally text-based so relationship changes can be code-reviewed and kept in sync with the schema. Publication artwork may restyle the diagram, but it must not change the underlying relationships.
