# Expected Results

These CSV files are deterministic fixtures derived from the synthetic teaching seed. They support manual and automated regression checks for high-value book examples.

The SQL assertion files in `tests/regression/` are the CI enforcement mechanism. If the seed data changes intentionally, update the fixtures and assertions together and document the change in `CHANGELOG.md`.
